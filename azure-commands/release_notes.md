#### The following enhancements have been made to the Azure Commands connector in version 1.0.1:

- Modified the login code to allow client applications without a bound subscription by enabling support for no
  subscriptions.